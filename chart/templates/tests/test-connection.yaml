apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fastapi-server.fullname" . }}-test-connection"
  labels:
    {{- include "fastapi-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fastapi-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
